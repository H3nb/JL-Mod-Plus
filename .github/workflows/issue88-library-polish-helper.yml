name: Issue 88 library polish helper

on:
  push:
    branches:
      - ui/harmonize-app-wide

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[issue88-library-polish-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: ui/harmonize-app-wide
          fetch-depth: 0

      - name: Harmonize library presentation
        run: python .github/scripts/issue88_library_polish.py

      - name: Commit patch
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "No changes produced" >&2
            exit 1
          fi
          git config user.name "H3nb"
          git config user.email "93023716+H3nb@users.noreply.github.com"
          git add app/src/main/java/ru/playsoftware/j2meloader/applist/LibraryComposeBridge.kt app/src/main/res/values/strings.xml app/src/main/res/values-in/strings.xml
          git commit -m "Harmonize library and app actions [issue88-library-polish-applied] [skip ci]"
          git push origin HEAD:ui/harmonize-app-wide
