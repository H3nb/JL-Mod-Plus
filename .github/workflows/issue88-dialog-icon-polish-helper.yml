name: Issue 88 dialog icon polish helper

on:
  push:
    branches:
      - ui/harmonize-app-wide

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[issue88-dialog-icon-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: ui/harmonize-app-wide
          fetch-depth: 0

      - name: Harmonize dialog icons and hierarchy
        run: python .github/scripts/issue88_dialog_icon_polish.py

      - name: Commit patch
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "No changes produced" >&2
            exit 1
          fi
          git config user.name "H3nb"
          git config user.email "93023716+H3nb@users.noreply.github.com"
          git add app/src/main/java/ru/playsoftware/j2meloader/applist/LibraryComposeBridge.kt app/src/main/java/javax/microedition/shell/RuntimeMenuCompose.kt
          git commit -m "Harmonize dialog icons and runtime menu [issue88-dialog-icon-applied] [skip ci]"
          git push origin HEAD:ui/harmonize-app-wide
